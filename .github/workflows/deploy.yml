name: Deploy to Firebase Hosting

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: 🚀 Build and Deploy
    runs-on: ubuntu-latest
    steps:
      - name: 📚 Checkout Code
        uses: actions/checkout@v4

      - name: 🐦 Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: 📦 Install Dependencies
        run: flutter pub get

      - name: 🏗️ Build Web (Staff)
        run: flutter build web --release --base-href / --output=build/web_staff --dart-define=APP_FLAVOR=staff

      - name: 🏗️ Build Web (Client)
        run: flutter build web --release --base-href / --output=build/web_client --dart-define=APP_FLAVOR=client

      - name: 🚀 Deploy Staff to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MAXIMUS_V2_7325F }}'
          channelId: live
          projectId: maximus-v2-7325f
          target: staff

      - name: 🚀 Deploy Client to Firebase
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_MAXIMUS_V2_7325F }}'
          channelId: live
          projectId: maximus-v2-7325f
          target: client
